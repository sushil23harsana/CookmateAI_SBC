# Copy to .env.local (optional — the default works for local dev).
#
# The ONLY thing the web app needs: where the Cookmate engine API runs.
# NEXT_PUBLIC_ vars are shipped to the browser — never put secrets here.
# ANTHROPIC_API_KEY lives in the ROOT .env (the API server reads it).
NEXT_PUBLIC_API_BASE=http://localhost:8787
